Extensions=[SQLite, SQLite viewer, material icons, C#, C# dev-kit, solution (like VS)]
Packages = [EFCore.Design, EFCore.Sqlite]
  
setx DOTNET_CLI_TELEMETRY_OPTOUT 1

dotnet new list

dotnet new webapi -h //-h contextual help

dotnet new sln // dotnet project container - solution file from template

dotnet new webapi -controllers -n API // webapi is template "API" is name we want use

dotnet sln add API //add project to Solution

dotnet sln list // check if project
 added

launchsetting.json ---> init file for project

models is entities in C#, vice-verse models=entities

Entity Framewrok = ORM = Translate code into SQL cmds for db opx.

[Querying, change tracking, saving, concurrecy, transaction, caching(L-1), Bulit-in conventions, configs, migrations] code-first or db-first (migrations)

DbContext (provided by EF) -> class --> main bridge b/w our entities and the DB.

LINQ queries. 

DbContext.Users.Add(new User{id=4, name="John"}) ->DB provider(sqlite, postgres, sql server)-> INSERT INTO USERS (ID,Name) Values(4,John)

dotnet restore // to refresh - if packages (nuget) changed

-->Create API/Data --->AppDbContext.cs (DbContext Class) 
// Data folder for all Data Access Related code

Derived class : Derived from class

public class AppDbContext : DbContext (UoW, Repo pattern //Provides abstraction from DB)--> provide with DBContextOptions to set conn String, etc.

// public DbSet<AppUser> Users { get; set; } //Users will be the table name in Db

Register DbContext in Program.cs as a service.
add connection string in appsettings.json //configuration after logging

-> Run EF Migrations

dotnet tool list -g 
dotnet tool install -g dotnet-ef // tool for migrations
dotnet ef // tool help

dotnet ef migrations add <Migration Name> -o Data/Migrations
dotnet ef migrations add InitialCreate -o Data/Migrations //-o output folder

-------CREATE OUR DB NOW------------
dotnet ef database update

create APIs in Controller. RULE: <ControllerName>sController e.g.: MembersController

NOTE: Any Service added in Program.cs are made available to use for Dependency Injection.

-------------ANGULAR SKELETON--------------
Extensions:[Angular Language, ESLint]
npm install -g @angular/cli
ng version

ng new client //client is project name
ng serve // to build and run

-index.html is default access page/ entry point page to any website.
-index.html has <app-root>
-the angular app renders in the app-root>
-any linked components are rendered where/in <router-outlet></router-outlet>
-app is a component.

@component({
selector:'app-root', //name how should it be called for use n html
imports:[array of imports],
templateUrl: './app.html', //the name of html file with view
styleUrl: './app.css' // the css file for that view
})

protected means also available to class children
"main.ts" --> ENTRY point of Angular application

linked editing vscode setting- for matching bracket.
developer reload window -> vscode refresh

provide to whole anglar app-. Use app.config.

provide HttpsClient module for talking to server- provideHttpClient()

=> methods to inject dependency in a component class:
1. private http = inject(HttpClient)
2. ctor(private http:HttpClient){} //OLD style

-> start with implementing ngOnInit=> class App implements OnInit
-> ngOnInit():void {} //things to-be performed at the start of appln
ctor vs ngOnIt() decide

http requests return Observable and we subscribe to it. [next, error, complete] => thus it auto unsubscribes


But for Observable streaming events -> use unsubscribe()
@for (member of members; track member.id) instead of *ngFor

-> reactive meaning that it responds to changes. 

ANGULAR SIGNALS instead of Zone.js for change detection and tracking
e.g.: 
protected members = signal<any>([]); has a set and get method; members.set(values)
members() //use in html calls members.get() internally 

better use function with and call it async in ngOnInit(
	this.members.set(await this.getMembers())
)

promises are not stream of data. and we don't subscribe to it.

Add Tailwind css from its page and daisy UI. 
Once you add Tailwind CSS generic does not work(h1, ul) but replaced by tailwind classes

add cert in ssl folder in angular and supply via angular.json in serve -options. use make cert for local CA.
----------------------Skeleton Done----------------
Auth, DTO, JWT, DRY - Inheritance | ASP.NET Identity
Serivices in c#, extension methods

Where do i start -> requirements (User Should be statements)
password hashing -> same password = same hash -> add SALTING

after upfdating any entity: "dotnet ef migrations add UserEntityUpdated" UserEntityUpdated is a txt message

ALWAYS UPDATE DB AFTER MIGRATIONS: dotnet ef database update
BaseAPi controller, derive future controllers from this rather than ControllerBase everywhere for DRY.
public class AccountController(AppDbContext context) : BaseApiController

public async Task<ActionResult<AppUser>> Register(string email, string displayName, string password) // Params received as queryString not in Body
but use DTO.

context.Users.AnyAsync(x=>x.Email.ToLower()== email.ToLower());

dotnet ef database drop //drop db

------------JWT----------- [header(algo, type), payload(claim), sign]

claims - nbf = notbefore
claims - exp = expires
calims - iat = issued at

Service = treate something as a service if it doesn't access our database or 3rd party service
			   ||			|| 
Implementation || Interface || Usage(use, test, share, show structure to use)
			   ||			|| 

Interface hides implementation just tells us what method you need to implement, what would that method accept as parameters and what it would return.

add service to Program.cs
use interface for DI. Inject IConfiguration to access Environment or config vars in a service class

var tokenKey = config["TokenKey"]; // from appsettings.json
var tokenKey = config["TokenKey"] ?? throw new Exception("Cannot get token key"); // ?? is null coalescing operator; if condition on left fails use right

Symmetric means use same key to encrypt and decrypt. 
Claims: Email, UserEntityUpdated
Then add authentication serivce in Program.cs (pkg: jwtBearer-> AddAuthentication()); then add to middleware

Extension methods: are written in "public static class AppUserExtensions" | contains public STATIC extension methods.

Static=> no need to instantiate class object using "new" when using it.

Cannot use dependency injection in Static classes, so use parameters.

public static UserDto ToDto(this AppUser user, ITokenService tokenService) IMPORTANT ===> as we add this, it mentions  the class that we are extending. Thus when use in the main class. it is called on the class object and only parameters except for the "this" needs to be passed. <=== IMPORTANT

Angular Structure /ref Angular Style guide in doc:
src/app - with app specific components
src/layout - comp. related to layout eg: navbar 
src/features - like members/accounts etc related components
src/core - services
src/shared - shared comps, used by more than one comp/feature
---------------------------
Project style guide LIFT principle: 
Locate - location of the folder
Identify - proper names to what it does
Flat - keep it flat meaning no sub dirs
Try to be DRY - Dont Repeat Yourself
-----------------
ng generate --help
ng g c // g= generate , c= component
ng g c nav --dry-run // --dry-run won't make changes just show where it create components. 
 
UPDATE "schematics" in Angular.json to skip-tests:true/false or a root=src

same for services too

add components in app.html where needed in design

flexboggy.com //learn flex
flex flex-row //move row-ward
flex flex-col //move col-ward
daisy ui gives theming

flex when used alone is default row-ward
---------
ng g service [name]           Creates a new service in your project. Services are used to encapsulate reusable logic, such as data access, API calls, or utility functions. This schematic simplifies   
                                the process of generating a new service with the necessary files and boilerplate code.       
daisyUI is pure html/css

Angular control statement
@if<condition>{<html>}@else{}

Events in any tag => (click)="logout()"

Observables and signals:
Observables are lazy collections of multiple values over time -> if not subscribed nothing will be produced -> newsletter example. TO STOP -> UNSUBSCRIBE

PROMISE (async,single-value(res or error), not-lazy, can't cancel, will give a val) vs observable(cancellable, streaming, transform with map, filter, reduce, with RxJS eg:http; only works if subscribed else nothing-lazy); 

or use toPromise(), if return a promise instead of subscribe.

-> subscribe has 3 parts (next action to do as data arrive, error, onComplete action) // HTTP are always complete so no unsubscribe needed. 

ASYN PIPE: in the html/template directly and no need for sub or unsub. " service.getMembers()| async"

vs SIGNALS [state management] not used with async. ==> notify components similar to useState hooks in React -> notifying all of its consumers. are getter function means call them to get value.
E.g.: count = signal(0) ; console.log(count()) | also count.set(value) or count.update(value=> value + 2)

-> performance, reevalute when dependencies change, auto-cleanup
-> synchronous state change - predictable

currentUser = signal<User | null>(null); the pipe is union operator could user type or null.

.pipe()
.tap() allows use to see incomning data without actually disturbing the stream

#Browsers persistence local storage

localStorage.setItem('user', JSON.stringify(user)); 
localStorage.removeItem('user');


@if (accountService.currentUser(); as user) //html

!! Refer Angular Design Style in its docs !!

ANGULAR COMPONENT COMMUNICATION -
|- Parent to child component:
	-1. using decorators - @Input({ required: true }) membersFromApp: User[] = []; // @ means its a decorator, 		declare passed value in parent html comp. on child-element -> in app.html in <app-home 				[membersFromApp]="members" />  syntax: [var_name_in_child]="var_name_in_parent()" () if its a 			signal.

	- 2. using signal - membersFromHome=input.required<User[]>(); //input is input-signal declared in 		child comp. [membersFromHome]="membersFromApp" in parent-html 

<<< app->home->register >>>
================================================================
|- Child to parent component:
	decorator is old so SHIFT to USING SIGNALS, 
	e.g.:  cancelRegister = output<type>() //output is from angular signals // Output to notify parent 	component (Home) about 	cancellation as boolean value. it has an "emit" EVENT -> cancelRegister.emit(value)

parent catching it => <app-register [membersFromHome]="membersFromApp" (cancelRegister)="showRegister($event)"> $evetn is its type bcoz its an event



